import 'package:flutter/material.dart';

class Climatempo extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return MaterialApp(
      title: 'Clima',
      debugShowCheckedModeBanner: false,
      home: Home(),
    );
  }
}